<!DOCTYPE html>
<html lang="en">
    <head>
        <title> About to Change</title>
        {{>head}}
    </head>

    <body>
        {{>navbar}}
        <div class="container" style="padding-left:200px; padding-top:20px">
            {{{body}}}
        </div>
        {{>scripts}}
    </body>
</html>

